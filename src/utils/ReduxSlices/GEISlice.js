import { createSlice } from "@reduxjs/toolkit";
const initialState={}
const GEISlice = createSlice({
    name:'gei',
    initialState,
    reducers:{}
})
export const {} = GEISlice.actions
export default GEISlice.reducer